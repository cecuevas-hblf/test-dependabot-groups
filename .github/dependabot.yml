version: 2
updates:
  - package-ecosystem: "terraform" # See documentation for possible values
    directory: "/dev"
    schedule:
      interval: "weekly"
      day: "tuesday"
      time: "14:30"
      timezone: "America/Mexico_City"
    allow:
      - dependency-name: "*"
        dependency-type: "all"
    groups:
      dev-providers-patch:
        patterns:
        - "@tfe*"
        update-types:
        - "patch"
        exclude-patterns:
        - "*module*"
      dev-providers-minor:
        patterns:
        - "@tfe*"
        update-types:
        - "minor"
        exclude-patterns:
        - "*module*"
      dev-providers-major:
        patterns:
        - "@tfe*"
        update-types:
        - "major"
        exclude-patterns:
        - "*module*"
